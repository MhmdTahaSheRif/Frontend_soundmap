import { RandomIconPipe } from './random-icon.pipe';

describe('RandomIconPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomIconPipe();
    expect(pipe).toBeTruthy();
  });
});
